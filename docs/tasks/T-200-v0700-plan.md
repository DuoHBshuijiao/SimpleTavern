# T-200 v0.700 计划总卡

- status: done
- area: frontend + backend + docs
- theme: 组件化与可观测性强化（行为不变）

## 背景

v0.600（含 v0.601 无障碍补强）已完成。`ChatPage.vue`（7166 行）已是编排层，`SettingsDrawer.vue`（6651 行）草稿耦合深。v0.700 在不改变行为的前提下建立组件测试基座、提炼低风险 composable，并扩展后端数据完整性与导入/导出可观测性。

## 子任务

### T-201 前端组件测试基座

- read_first：`frontend/package.json`、`frontend/vite.config.ts`、`frontend/src/components/ThemedCheckbox.vue`
- acceptance：装 `@vue/test-utils` + `happy-dom`；可挂载简单 SFC；新增至少一个组件测试；`npm run test` 通过。

### T-202 useChatSearch

- read_first：`frontend/src/views/ChatPage.vue`（会话搜索段）、`frontend/src/composables/`
- acceptance：会话内搜索状态/方法提炼为 `useChatSearch`；ChatPage 行为不变；配单测；build + test 通过。

### T-203 useImageStickyBinding

- acceptance：图片粘性绑定/回退提炼为 `useImageStickyBinding`；行为不变；配单测。

### T-204 useForkLineage

- acceptance：分叉血缘提炼为 `useForkLineage`；行为不变；配单测。

### T-205 数据完整性扫描扩展

- read_first：`backend/app/services/data_integrity.py`、`backend/app/routes/data_integrity.py`、`frontend/src/api/dataIntegrity.ts`、`frontend/src/components/StartupIntegrityWatcher.vue`
- acceptance：扫描扩展到 `settings.json`、`characters/`、`worldbooks/`；新增 orphan 引用检测；后端测试覆盖；前端能展示新增类别。

### T-206 import/export warning 补强

- read_first：`backend/app/routes/import_export.py`、`frontend/src/composables/useSettingsImport.ts`
- acceptance：TXT/JSONL 导入 warning 透传；合并顶层与 MVU 兼容 warning；导出跳过项告知；后端测试覆盖。

### T-207 收尾验证

- acceptance：`frontend npm run test` + `npm run build` + `backend pytest` 全通过；更新 CHANGELOG/ROADMAP/CURRENT/LAST_HANDOFF/版本；生成中文提交命令。

## verify

```powershell
cd E:\SimpleTavern\frontend
npm run test
npm run build
cd E:\SimpleTavern\backend
python -m pytest tests/ -q
```

## next_hint

逐子任务推进，每个子任务完成后跑相关验证；全部完成后更新 `docs/state/LAST_HANDOFF.md`。
