# GLM-TTS 本地模型使用指南

这份文档对应 SimpleTavern 前端里用户实际能看到的所有 GLM-TTS 本地相关输入框、按钮、开关和提示文案，按界面路径逐项说明怎么填、什么时候填、哪些控件在本地模式下有用、哪些只是保留的通用控件。

适用对象：

- 在 Windows 上运行 SimpleTavern
- 已经单独准备好可运行的 GLM-TTS 仓库
- 希望在前端设置页中把 GLM-TTS 作为本地 TTS 服务使用

---

## 1. 你最终会在前端做什么

完整流程只有四步：

1. 在全局设置里开启文字转语音。
2. 在 API 预设里新建一个 TTS 预设，并把 TTS 提供商切到 GLM-TTS（本地）。
3. 在这个预设里填写本地仓库路径、端口、启动方式，并手动添加参考音色。
4. 在会话设置里选择这个 TTS 预设对应的模型，并把角色或用户身份绑定到你刚刚添加的音色。

完成后，聊天界面的朗读功能就会使用本地 GLM-TTS 合成语音。

---

## 2. 使用前准备

在打开前端之前，请先确认下面几件事：

- 你已经有一个可运行的 GLM-TTS 仓库目录。
- 这个目录里存在 run_api_gpu.ps1。
- 模型权重、Python 依赖、CUDA 环境已经按 GLM-TTS 官方说明准备好。
- 你知道本地 API 要监听的端口，默认通常是 8088。
- 你手里已经有至少一段参考音频，以及这段音频对应的准确转写文本。

推荐准备：

- 仓库路径示例：E:\GLM-TTS
- 本地 API 地址示例：http://127.0.0.1:8088
- 参考音频路径示例：E:\voices\paimon_ref.wav
- 参考文本示例：今天天气不错，我们开始吧。

---

## 3. 全局设置页：文字转语音（TTS）

界面路径：设置抽屉 → 全局设置 → 文字转语音（TTS）

这一块和 GLM-TTS 本地直接相关的控件如下。

| 前端文案 | 控件类型 | 作用 | GLM-TTS 本地怎么用 |
| --- | --- | --- | --- |
| 启用文字转语音 | 开关 | 打开整个 TTS 功能 | 必须开启，否则聊天页的会话级 TTS 配置不会生效 |
| 缓存上限（MB） | 数字输入框 | 控制本地音频缓存上限 | 按你磁盘空间决定，常见可填 512、1024、2048 |
| 清空缓存 | 按钮 | 删除已缓存的合成音频 | 仅影响缓存，不影响 GLM-TTS 模型本身 |

你会看到的相关提示：

- 已开启：启用语音合成功能
- 开启后可在聊天界面使用语音合成功能。需在 API 预设中至少配置一个 TTS 服务预设，可选 MiniMax 或 GLM TTS。

建议：

- 第一次接入时，先把缓存上限设大一点，方便排查是否是重复合成还是缓存复用。
- 如果你换了参考音色但播放还是旧声音，可以先点清空缓存。

---

## 4. API 预设页：把预设切成 GLM-TTS（本地）

界面路径：设置抽屉 → 预设 → 选择或新建一个预设

### 4.1 通用预设字段

下面这些控件在所有 TTS 预设里都能看到。

| 前端文案 | 控件类型 | 作用 | GLM-TTS 本地怎么填 |
| --- | --- | --- | --- |
| 预设名称 | 文本输入框 | 给当前预设命名 | 建议写成 GLM-TTS 本地 或 GLM-TTS 8088 |
| 作为 TTS 服务 | 复选开关按钮 | 把这个预设标记为 TTS 预设 | 必须勾上 |
| TTS 提供商 | 下拉选择 | 选择 TTS 提供商 | 选择 GLM-TTS（本地） |
| API 基础地址 | 文本输入框 | TTS API 的 Base URL | 通常填 http://127.0.0.1:8088 |
| API Key | 文本输入框 | 云端服务鉴权用 | 本地模式可留空 |
| 模型列表 | 列表区域 | 给这个预设挂可选模型名 | 建议至少保留一个 glm-tts |
| 从 API 获取并筛选 | 按钮 | 拉取候选模型 | 本地模式下不建议依赖，建议手动添加 |
| 手动输入模型名... | 输入框 | 手动添加模型名 | 推荐手动输入 glm-tts |

重要说明：

- API Key 字段虽然还会显示，但在 GLM-TTS（本地）模式下不是必填项。
- 模型列表在本地模式里主要用于前端绑定和会话选择，不是用来驱动 GLM-TTS 下载模型。
- 如果你改了本地端口，请把 API 基础地址也一起改掉，保持两者一致。

最稳妥的填法：

- 预设名称：GLM-TTS 本地
- TTS 提供商：GLM-TTS（本地）
- API 基础地址：http://127.0.0.1:8088
- API Key：留空
- 模型列表：至少有一个 glm-tts

### 4.2 你会看到的状态文字

切到本地模式后，预设卡片里会显示：

- 当前提供商：GLM-TTS（本地）

这行字仅用于确认当前预设确实已经切到本地提供商。

---

## 5. GLM-TTS（本地）专属配置区

当 TTS 提供商选择为 GLM-TTS（本地）后，界面会额外出现本地专属输入项。

| 前端文案 | 控件类型 | 作用 | 应该怎么填 |
| --- | --- | --- | --- |
| 仓库路径 | 文本输入框 | 指向你的 GLM-TTS 仓库根目录 | 填包含 run_api_gpu.ps1 的目录，例如 E:\GLM-TTS |
| 端口 | 数字输入框 | 指定本地 API 监听端口 | 默认填 8088；如果你自己改过，就填你的端口 |
| 托管启动 | 切换按钮 | 控制由程序启动还是你手动启动 | 二选一，根据你的习惯决定 |
| 由程序启动 | 按钮状态 | 表示首次合成前会自动尝试启动 GLM-TTS | 适合单机固定环境 |
| 手动启动 | 按钮状态 | 表示你自己先运行 GLM-TTS API，再由前端连接 | 适合你自己习惯单独开服务 |

这一区下面还会有一行说明：

- 由程序启动会在首次合成前自动运行 run_api_gpu.ps1；手动启动需自行启动本地 API。

### 5.1 两种启动方式怎么选

选“由程序启动”时：

- 必须填写仓库路径。
- 第一次真正发起语音合成时，SimpleTavern 会尝试自动运行 run_api_gpu.ps1。
- 如果模型加载很慢，第一次合成等待时间会比较长。

选“手动启动”时：

- 你需要自己先在 GLM-TTS 仓库里启动本地 API。
- API 基础地址必须能访问到你已经启动的服务。

推荐：

- 只在固定 Windows 机器上自己使用，选“由程序启动”。
- 你已经有独立脚本或习惯手动开服务，选“手动启动”。

---

## 6. 音色列表与本地参考音色

GLM-TTS 本地模式和云端 TTS 最大的区别，是它没有远程可拉取的官方音色库。你必须自己维护参考音色。

### 6.1 音色列表区域里会看到什么

| 前端文案 | 控件类型 | 作用 | 本地模式说明 |
| --- | --- | --- | --- |
| 音色列表 | 标题 | 展示当前预设里已保存的音色条目 | 这些条目就是后续角色/用户可选的 voice_id |
| 全选 | 按钮 | 选中全部音色条目 | 用于批量删除 |
| 清空选择 | 按钮 | 取消当前选择 | 仅影响当前选中状态 |
| 删除所选 | 按钮 | 删除选中的音色条目 | 会从预设里移除 |
| 清空全部 | 按钮 | 删除整个音色目录 | 谨慎使用 |
| 请在下方添加本地参考音色 | 提示文案 | 当前还没有本地音色 | 说明你还没创建任何 voice_id |

注意：

- 本地模式下，音色列表右上角的“从 API 获取并筛选”按钮不会出现。
- 这是正常行为，因为 GLM-TTS 本地没有可直接拉取的远程音色库。

### 6.2 这个输入框仍然可见，但它只是快速创建壳条目

界面里仍然会有一个输入框：

- 手动输入 voice_id 后按回车添加…

在 GLM-TTS（本地）模式下，这个框可以用，但它只会先创建一个最简音色条目。你后面仍然要在下方把参考音频路径和参考文本补全，否则这个 voice_id 无法真正用于合成。

如果你不想分两步，建议直接使用下面“添加本地参考音色”区域一次填完整。

---

## 7. 添加本地参考音色：逐项填写说明

界面路径：同一个 TTS 预设编辑区 → 添加本地参考音色

这是 GLM-TTS 本地接入里最关键的一块。

| 前端文案 | 控件类型 | 作用 | 填写建议 |
| --- | --- | --- | --- |
| 添加本地参考音色 | 区块标题 | 用来新增一个可复用的本地音色 | 每个音色条目都要维护完整 |
| 音色 ID（唯一标识） | 文本输入框 | 内部 voice_id，后面在角色/用户绑定时会用到 | 用英文、数字、下划线最稳，例如 paimon_local |
| 音色名称（显示用） | 文本输入框 | 仅用于前端展示 | 可以写成 派蒙、本地女声 A 等 |
| 参考音频路径（wav/flac 绝对路径） | 文本输入框 | 指向本机参考音频文件 | 必须填绝对路径，建议不要用相对路径 |
| 参考音频对应转写文本（推荐填写） | 文本输入框 | 填参考音频里的准确朗读内容 | 尽量逐字一致，不要大意省略 |
| 添加音色 | 按钮 | 把当前草稿写入音色列表 | 点完后会进入上方音色列表 |

推荐写法示例：

- 音色 ID：paimon_local
- 音色名称：派蒙
- 参考音频路径：E:\voices\paimon_ref.wav
- 参考音频对应转写文本：旅行者，接下来要去哪里呀？

填写原则：

- 参考音频必须是你本机上真实存在的文件。
- 路径建议使用不带引号的 Windows 绝对路径。
- 参考文本越准确，合成稳定性和相似度通常越好。
- 即使前端写着“推荐填写”，本地模式下也强烈建议不要留空。

### 7.1 已添加音色的二次编辑区

每添加一个本地音色后，下方还会出现对应的详情编辑项。

你会看到这些字段：

- 参考音频路径
- 参考转写文本

它们的作用是：

- 允许你在音色创建之后继续修正路径
- 允许你修正参考文本，避免一开始填错

建议：

- 新建后立刻再检查一遍路径和文本是否准确。
- 如果你是通过“手动输入 voice_id 后按回车添加…”先创建的条目，一定要回来补完这两个字段。

---

## 8. 会话设置页：真正让聊天使用这个本地 TTS

界面路径：设置抽屉 → 当前会话 → 文字转语音

这一块决定当前聊天到底用哪个 TTS 预设、哪个模型、哪个音色。

### 8.1 会话级 TTS 控件总览

| 前端文案 | 控件类型 | 作用 | GLM-TTS 本地怎么用 |
| --- | --- | --- | --- |
| TTS 模型 | 下拉或可输入选择框 | 选择当前会话使用的 TTS 模型 | 选你在预设里填的 glm-tts |
| 当前预设：某某 · GLM-TTS（本地） | 状态文案 | 显示当前模型关联到了哪个 TTS 预设 | 用来确认会话确实走的是本地预设 |
| 自动朗读范围 | 四段按钮 | 控制消息是否自动朗读 | 按需要选 关、角色、用户、全部 |
| 朗读间隔（秒） | 数字输入框 | 控制连续播放时的间隔 | 常见填 0、0.5、1.0 |
| 文本后处理 | 功能区 | 合成前先用文本模型润色朗读文本 | 可选，不是必须 |
| 启用文本后处理 | 开关按钮 | 打开或关闭文本预处理 | 只有你确实需要规范化文本时才建议开 |
| 注入英文情绪标签 | 开关按钮 | 给 MiniMax 注入兼容标签 | 在 GLM-TTS 本地下会禁用，不用管 |
| 后处理目标语言 | 文本输入框 | 指示后处理阶段输出什么语言 | 想强制转成中文或英文时填写 |
| 选择文本后处理模型... | 下拉选择框 | 选择用于预处理的普通文本模型 | 从普通文本预设里选，不是 TTS 模型 |
| 角色音色 | 绑定列表 | 把每个角色绑定到一个 voice_id | 必须至少给要朗读的角色绑定音色 |
| 用户音色 | 绑定列表 | 把每个用户身份绑定到一个 voice_id | 如果要朗读用户消息，也要绑定 |

### 8.2 TTS 模型该怎么选

最推荐的做法：

- 在预设模型列表里手动保留一个 glm-tts。
- 在会话的 TTS 模型下拉框里直接选 glm-tts。

为什么：

- 这样最清晰，后续你一眼就能看出这个会话绑定的是本地 GLM-TTS。
- 前端会同时显示当前预设：你的预设名 · GLM-TTS（本地），便于排查是不是选错了预设。

### 8.3 自动朗读范围怎么选

四个按钮对应的行为：

- 关：不自动朗读，只能手动播放。
- 角色：只自动朗读 assistant 或角色消息。
- 用户：只自动朗读 user 消息。
- 全部：角色和用户消息都会自动朗读。

如果你只想给 NPC 配音，通常选“角色”就够了。

### 8.4 文本后处理在本地模式下怎么理解

文本后处理不是 GLM-TTS 本地模型的一部分，它只是 SimpleTavern 在真正调用 TTS 前，先用一个普通文本模型把文本清洗、翻译或规范化。

对 GLM-TTS（本地）来说：

- 可以开启文本后处理。
- 可以填写后处理目标语言。
- 可以选择一个普通文本模型来做预处理。
- 注入英文情绪标签这个选项会禁用，这是正常的。

建议：

- 普通中文对话一般可以先不开。
- 如果聊天里有很多 Markdown、动作括号、混合语言，再考虑开启。

### 8.5 角色音色和用户音色怎么填

这里最常见的操作，就是把你在预设里创建的 voice_id 分配给角色或身份。

比如：

- 角色“派蒙” → paimon_local
- 角色“旁白” → narrator_local
- 用户身份“默认用户” → user_local

只要绑定完成，聊天页在朗读对应消息时就会拿这个 voice_id 去查找你预设里保存的参考音频路径和参考文本。

---

## 9. 推荐的最小可用配置

如果你只想先跑通，不想一开始就配很多项目，可以按下面最小配置走。

### 9.1 全局设置

- 启用文字转语音：开启
- 缓存上限（MB）：1024

### 9.2 TTS 预设

- 预设名称：GLM-TTS 本地
- 作为 TTS 服务：开启
- TTS 提供商：GLM-TTS（本地）
- API 基础地址：http://127.0.0.1:8088
- API Key：留空
- 模型列表：glm-tts
- 仓库路径：E:\GLM-TTS
- 端口：8088
- 托管启动：由程序启动 或 手动启动 二选一

### 9.3 参考音色

- 音色 ID：paimon_local
- 音色名称：派蒙
- 参考音频路径：E:\voices\paimon_ref.wav
- 参考音频对应转写文本：旅行者，接下来要去哪里呀？

### 9.4 会话设置

- TTS 模型：glm-tts
- 自动朗读范围：角色
- 角色音色：把目标角色绑定到 paimon_local

这样就能跑起来。

---

## 10. 建议的日常使用顺序

每次新建本地音色或新开一个会话时，按这个顺序做最省事：

1. 先确认全局“启用文字转语音”已经打开。
2. 再确认当前会话的 TTS 模型显示的当前预设是 GLM-TTS（本地）。
3. 检查角色音色或用户音色是否已经绑定到正确的 voice_id。
4. 如果使用“由程序启动”，第一次朗读时耐心等模型加载完成。
5. 如果声音不对，先检查预设里这个 voice_id 的参考音频路径和参考文本。
6. 如果改过音色但听起来像旧版本，清空 TTS 缓存后再试。

---

## 11. 常见提示和它们真正表示什么

下面这些提示都是用户可能在界面或朗读过程中看到的。

### 11.1 请先在“全局设置 → 文字转语音（TTS）”里开启 TTS，当前会话配置才会生效。

含义：

- 你已经在会话里配置了 TTS，但总开关还没打开。

处理：

- 回到全局设置，把“启用文字转语音”打开。

### 11.2 还没有可用的 TTS 模型。请先在 API 预设中把目标预设标记为 TTS 服务并获取模型列表。

含义：

- 当前没有任何可供会话选择的 TTS 模型。

处理：

- 进入预设页，确认当前预设勾了“作为 TTS 服务”。
- 在模型列表里至少手动添加一个 glm-tts。

### 11.3 当前预设还没有已拉取的音色列表。你可以回到 API 预设里点击“从 API 获取并筛选”勾选音色，也可以直接手输 voice_id。

对 GLM-TTS（本地）的正确理解：

- 这段文案是通用提示。
- 本地模式不依赖“从 API 获取并筛选”拉取音色。
- 你应该回到预设页，自己添加本地参考音色，或者先手输 voice_id 再补全参考音频路径和参考文本。

### 11.4 当前角色未配置音色。

含义：

- 这个角色还没有绑定 voice_id。

处理：

- 去当前会话的“角色音色”区，把角色绑定到你预设里的某个本地音色。

### 11.5 当前用户身份未配置音色。

含义：

- 你开启了用户消息朗读，但当前身份没有绑定 voice_id。

处理：

- 去“用户音色”区配置。

### 11.6 当前消息没有可朗读的文本。

含义：

- 这条消息可能是空内容，或者显示文本为空。

处理：

- 先确认消息正文里确实有可朗读内容。

### 11.7 已启用托管启动，但未配置 GLM-TTS 仓库路径。

含义：

- 你把“托管启动”切到了“由程序启动”，但仓库路径没填。

处理：

- 回到预设页，把“仓库路径”填完整。

### 11.8 GLM-TTS 本地托管启动失败，请检查仓库路径、模型依赖和端口配置。

含义：

- 程序尝试帮你启动 run_api_gpu.ps1，但没有成功。

优先检查：

- 仓库路径是否正确
- run_api_gpu.ps1 是否存在
- 端口是否被别的程序占用
- GLM-TTS 模型依赖和权重是否已经准备好

---

## 12. 本地模式下哪些控件是“会看到，但不建议依赖”的

为了避免误解，这里单独列出来。

### 12.1 API Key

会看到，但本地模式通常不用填。

### 12.2 模型列表里的“从 API 获取并筛选”

会看到，但 GLM-TTS 本地并不提供稳定的模型列表拉取流程。最实用的做法仍然是手动写一个 glm-tts。

### 12.3 手动输入 voice_id 后按回车添加…

会看到，也可以用，但它只适合先快速建一个条目。对 GLM-TTS 本地来说，后续仍然必须补完：

- 参考音频路径
- 参考转写文本

否则这个 voice_id 只是名字存在，实际无法完成本地零样本合成。

---

## 13. 最终检查清单

如果你已经全部填完，但还没开始用，最后核对一遍下面这些点：

- 全局“启用文字转语音”已开启。
- 预设已勾选“作为 TTS 服务”。
- TTS 提供商已切到 GLM-TTS（本地）。
- API 基础地址和端口一致。
- 如果选了“由程序启动”，仓库路径已经正确填写。
- 模型列表里至少有一个 glm-tts。
- 音色列表里至少有一个本地音色条目。
- 每个要使用的本地音色都已经填好参考音频路径和参考转写文本。
- 当前会话的 TTS 模型已经选中 glm-tts。
- 要朗读的角色或用户身份已经绑定到正确的 voice_id。

这些条件都满足后，GLM-TTS 本地模式就可以稳定使用了。